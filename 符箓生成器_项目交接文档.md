# 符箓生成器项目交接文档

## 项目概述
符箓生成器是一个基于八字五行分析的道教符箓生成工具，已上线至 https://ai-newland.com/fu.html

**项目特色**：
- ⚡ 传统文化 × AI生成
- 📱 专属符图屏保
- 🔮 基于五行八卦算法
- 💰 付费功能（弹窗模式）

## 已完成功能 ✅

### 核心算法
- ✅ 八字算法（公历→天干地支→五行）
- ✅ 五行分析（补缺/财星/印星/桃花星）
- ✅ 八卦推算

### 用户界面
- ✅ 网页 UI 界面
- ✅ 付费弹窗功能
- ✅ 已正式上线

## 项目结构
```
/var/www/fu/                     # 服务器上的项目目录
├── index.html                    # 主页面（线上版本）
└── images/                      # 图片资源目录
    └── payment/                 # 付款相关图片
```

**服务器信息**：
- 域名：ai-newland.com
- 路径：/var/www/fu/
- 已上线：https://ai-newland.com/fu.html

## 当前技术实现

### 符图绘制方式
```javascript
// 当前使用Canvas绘制简单线条
function drawTalismanLines(ctx, cx, sY, color, glow) {
  if(glow){ctx.shadowColor=color;ctx.shadowBlur=15;}
  ctx.strokeStyle=color;ctx.fillStyle=color;ctx.lineWidth=4;ctx.textAlign="center";
  ctx.beginPath();ctx.moveTo(cx,sY);ctx.lineTo(cx,sY+200);ctx.stroke();
  ctx.beginPath();ctx.arc(cx,sY-10,25,0,Math.PI*2);ctx.fill();
  ctx.lineWidth=3;
  for(let i=0;i<4;i++){
    const y=sY+30+i*50;
    ctx.beginPath();ctx.moveTo(cx-80,y);ctx.quadraticCurveTo(cx-40,y-15,cx,y);
    ctx.quadraticCurveTo(cx+40,y+15,cx+80,y);ctx.stroke();
  }
  ctx.beginPath();ctx.moveTo(cx-40,sY+220);ctx.lineTo(cx+40,sY+220);ctx.lineTo(cx,sY+270);ctx.closePath();ctx.fill();
  ctx.shadowBlur=0;
}
```

**问题分析**：
- Canvas绘制的符图过于简陋，缺乏专业符箓的复杂元素
- 缺少符胆、令牌、云篆等专业道教元素
- 视觉效果不够精美，用户体验不佳

### 核心功能模块
1. **八字转换算法**：将公历日期转换为天干地支
2. **五行分析**：分析八字中的五行缺失、财星、印星、桃花星
3. **符箓生成**：根据五行分析结果生成对应的符箓
4. **用户界面**：简洁的表单输入和结果展示

**用户输入项**：
- 姓名
- 出生日期
- 出生时辰
- 符类型选择
- 风格选择

## 待完成任务 🔴

### P0 优先级（最高）
#### 1. 设计符文素材
**任务描述**：请专业国学/道教符箓设计师手绘15张符文素材
**具体要求**：
- 5种符文类型 × 3种风格 = 15张素材
- 符胆、令牌、云篆等专业元素
- 高清PNG格式，透明背景

**符文类型**：
1. 生肖符（鼠、牛、虎、兔、龙、蛇、马、羊、猴、鸡、狗、猪）
2. 五行符（金、木、水、火、土）
3. 吉祥符（福、禄、寿、喜、财）
4. 避邪符（太岁、五鬼、灾厄、疾病、小人）
5. 事业符（文昌、功名、贵人、贵人、贵人）

**风格要求**：
- 传统风格：古法绘制，符合道教传统
- 现代风格：简化设计，适合现代审美
- 创意风格：融合现代元素，创新设计

#### 2. 代码替换
**任务描述**：将Canvas绘制替换为图片叠加
**具体操作**：
```javascript
// 替换前：使用Canvas绘制简单线条
function drawTalismanLines(ctx, cx, sY, color, glow) {
  // 现有的Canvas绘制代码
}

// 替换后：使用专业设计师手绘的符文图片
function drawTalismanImage(ctx, type, style) {
    const img = document.getElementById(`talisman-${type}-${style}`);
    if (img) {
        ctx.drawImage(img, 0, 0, 1080, 1920);
    }
}

// 在drawTalisman()函数中调用
function drawTalisman() {
    const canvas = document.getElementById("talismanCanvas");
    const ctx = canvas.getContext("2d");
    const W = 1080, H = 1920;
    
    ctx.clearRect(0, 0, W, H);
    
    // 使用图片替换Canvas绘制
    drawTalismanImage(ctx, state.type, state.style);
    
    // 其他文字和元素仍然使用Canvas绘制
    // ... 其他代码
}
```

### P1 优先级（中等）
#### 1. 更新收款信息
**任务描述**：替换收款码图片和客服微信
**文件位置**：`/var/www/fu/images/payment/`
**需要替换**：
- 收款码图片（当前使用AI Newland的收款码）
- 客服微信二维码（当前为空或占位符）
- 客服微信号文字（当前为空或占位符）

**注意**：需要确保图片路径正确，与HTML中的引用一致

### P2 优先级（较低）
#### 1. 小程序版本开发
**任务描述**：开发微信小程序版本
**技术栈**：
- 微信小程序原生开发
- 复用现有算法逻辑
- 简化用户界面

## 技术说明

### 依赖项
- 纯HTML/CSS/JavaScript实现
- 无框架依赖
- 兼容现代浏览器

### 数据处理
- 所有计算在前端完成
- 无需后端服务器
- 用户数据不上传到服务器

### 样式说明
- 响应式设计
- 移动端适配
- 简洁的道教风格UI

## 接手开发指南

### 1. 服务器环境准备
```bash
# 连接到服务器
ssh root@ai-newland.com

# 进入项目目录
cd /var/www/fu/

# 安装依赖（如果需要）
# 当前项目无需依赖，纯HTML/CSS/JavaScript

# 启动本地测试服务器（可选）
python -m http.server 8000
```

### 2. 运行项目
- **线上版本**：https://ai-newland.com/fu.html
- **本地测试**：http://localhost:8000/index.html

### 3. 修改要点
- **主要修改**：`index.html` 文件中的JavaScript代码（当前所有代码都在一个HTML文件中）
- **样式修改**：`<style>` 标签内的CSS代码
- **图片资源**：`/var/www/fu/images/` 目录
- **付费功能**：`showPayModal()` 和 `verifyCode()` 函数

### 4. 测试方法
- 输入不同的出生日期
- 验证五行分析结果
- 检查符箓生成效果
- 测试付费弹窗功能

## 注意事项

1. **符文素材质量**：必须使用专业设计师手绘的符文，避免使用简单的Canvas线条
2. **道教文化准确性**：确保符文符合道教传统，避免文化错误
3. **用户体验**：保持界面简洁，符合道教文化氛围
4. **法律合规**：确保符文内容不涉及封建迷信，符合相关规定

**免责声明**：
- 本工具基于传统文化五行八卦算法，仅供娱乐参考
- 不构成任何预测或建议
- 用户需自行承担使用责任

## 技术细节

### 核心算法
```javascript
// 八字计算函数
function calcBazi(y, m, d, h) {
    // 天干地支计算
    const yg = (y - 1900 + 6) % 10;
    const yz = (y - 1900) % 12;
    // ... 其他计算逻辑
}
```

### 符箓类型
```javascript
const TALISMAN_TYPES = {
    peace: { target: "weakest", label: "平安符", icon: "🛡️", blessing: "岁岁平安 诸事顺遂" },
    wealth: { target: "wealthStar", label: "招财符", icon: "💰", blessing: "财源广进 富贵盈门" },
    study: { target: "resourceStar", label: "文昌符", icon: "📚", blessing: "金榜题名 学业有成" },
    love: { target: "木", label: "桃花符", icon: "💕", blessing: "良缘天成 喜结连理" },
    health: { target: "strongest", label: "健康符", icon: "🏥", blessing: "身体康健 百病不侵" }
};
```

### 三种绘制风格
1. **道教风格（taoist）**：传统道教符箓样式
2. **国潮风格（guochao）**：现代国潮设计
3. **水墨风格（ink）**：传统水墨画风格

## 联系信息

如有任何问题，请联系：
- 项目负责人：[待补充]
- 设计师：[待补充]
- 技术支持：[待补充]

## 版本信息

- 当前版本：v1.0
- 上线时间：2026年7月
- 最后更新：2026年7月3日